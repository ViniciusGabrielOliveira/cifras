export const environment = {
  production: true,
  cifrasApiUrl: 'https://cifras-api.onrender.com',  // ajustar após o deploy
  cifrasApiKey: 'SUBSTITUA_PELA_CHAVE_DO_RENDER',    // não commitar a chave real
};
